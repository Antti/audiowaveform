# Preserve upstream license texts byte-for-byte, including their whitespace.
third-party/*.txt -text -whitespace
# The generated fixture manifest checksums this specification's exact bytes.
contract/vectors.json text eol=lf
# Even malformed audio fixtures can resemble text; their bytes are contractual.
tests/fixtures/** -text
